<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Android 11+ 包可见性:默认 app 看不到其它 app 的 intent-filter,
        Intent.resolveActivity / PackageManager.queryIntentActivities 会返回空。
        声明 queries 让"用文本编辑器打开附件"链路(MessageFileAttachments → ACTION_VIEW)
        能正确解析到目标 app。
        声明的 MIME 与 SupportedTextFileTypes.MIME_WHITELIST 对齐,加 text/* 兜底
        覆盖白名单外的 text/x-* 子类型(如 text/x-kotlin / text/x-csrc)。
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="text/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/json" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/xml" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/x-yaml" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/toml" />
        </intent>
    </queries>

    <application
        android:name=".NutTavernApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NutTavern"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.NutTavern">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FileProvider:把 chat-files/ 子目录(ConversationRepository.CHAT_FILE_DIRECTORY)
            通过 content:// URI 暴露给系统 ACTION_VIEW,让用户在历史气泡里点药丸能用文本编辑器打开。

            authority 必须与代码侧 FileProvider.getUriForFile 一致(${applicationId}.fileprovider)。
            grantUriPermissions=true 让我们能用 FLAG_GRANT_READ_URI_PERMISSION 把读权限临时授给目标 app。
            只暴露 chat-files/,不开整个 filesDir,避免 chat-images / personas / 数据库文件被外泄。
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>
